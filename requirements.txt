flask>=2.0
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
numpy>=1.24

# Optional. Enables parquet output alongside CSV in
# trip_extraction.py and match_factor.py. Deliberately not
# required: it is ~100 MB and the no-VPN public demo does not
# need it. Both modules detect it and degrade to CSV only.
# pyarrow>=15
