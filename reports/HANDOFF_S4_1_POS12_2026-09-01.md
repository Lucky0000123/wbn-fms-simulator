# HANDOFF — WBN FMS Haulage Simulator, Scenario 4.1 (S7) with LD → POS 12
Date: 2026-09-01. Last commits: `67bdc04`, then the IWIP-trips fix (pushed to BOTH remotes: origin + mirror).

## The project
- **Repo**: `/Users/lucky/wbn-fms-simulator` — Flask backend + vanilla JS frontend.
- **Run**: `.venv/bin/python serve.py` → http://localhost:5055 (port 5055, NOT 5000).
- **What it does**: simulates nickel-ore haulage for WBN (trucks = "DT", contractors RIM/SMA/IWIP + tenant traffic) and produces monthly/yearly haulage plans as Excel workbooks. A calibrated hybrid congestion model (BPR + bunching, backtest R² 0.926) prices trips/DT per route; a priority waterfall allocates fleet.
- **Key files**:
  - `scenario_api.py` — scenarios (pit×month targets), waterfall allocator, draft-plan builder.
  - `monthly_api.py` — year board API, Excel export (`/api/monthly/export-year?year=2026&day=7`), flat-fleet park solver `_ld_year_adjustment()`.
  - `simulator_api.py` — congestion model, weighbridge assignment (`wb_assign_rows`, tenants included, max 3 WB/path).
  - `static/js/plan_sap_target.js` — THE allocator the UI uses (Check capacity → Allocate DT → Save). `plan_scenario.js` — load/save. `planning_rules.js` + `planning_rules.md` — the rulebook.
  - Saved plans: `data/saved_plans/2026-MM-DD.json` (gitignored). Scenarios: `data/scenarios/S*.json`. Yearly matrix: `data/monthly_plans/yearly_matrix.json`.
  - Gates: `scripts/check_scenarios.py` (J72), `scripts/check_qa_2026_08_25.py` (51 checks). Both must pass.

## Core domain rules (owner's law — do not violate)
1. **Priorities**: P1 SAP → P2 LIM-TOS → P3 LIM-LD. LD gets the leftover fleet.
2. **Contractor walls**: BLB pit = RIM only, KR = SMA, TF open. Trucks never change owner.
3. **SAP routing**: each pit sends ~2,000 t/day to its POS buffer (BLB→POS 14, KR/TF→POS 12), rest DIRECT to FeNi.
4. **POS is transit**: inbound tonnes leave on IWIP trucks, input = output. IWIP is not contractor fleet (`foreign:true`).
5. **One clock**: never mix before/after-adjustment numbers in one table. Month targets must sum to the sales line every surface divides by.
6. **The YEAR is the judged line** (103% band). Fleet adjustment = **PARK ONLY** at ONE flat level all year (the purchase-decision number). Never redistribute surplus into short months.
7. Fleet never shrinks: Sep 581 / Oct 931 / Nov 1280 / Dec 1281 (RIM 961 + SMA 320 in Dec). All remaining trucks go on LD past target.
8. **Scenario = day slot**: day-01=S1, 03=S3.0.1, 04=S3.0.2, 05=S3.1.1, 06=S3.1.2, **07=S7 (4.1)**. One workbook = one scenario.

## Scenario 4.1 (what we built this session)
From the manager's 33-sheet mine plan `20260828 ... Scenario 3 (Without IPPKH6)_LimRec80%_R01.xlsx`, reconciled to the tonne:
- **SAP 6,383,425** = ROM 6,541,121 × 88% recovery (TOFU grizzly keeps ~10% in pit) + 627,239 HGS stock.
- **LIM-TOS 4,581,137** = fresh limonite, direct to Huafei.
- **LIM-LD 6,035,439** = stockpile reclaim. **TOTAL 17,000,001**.
- **Route change (Huafei meeting, owner 2026-08-31): ALL LD goes TF>POS 12** (POS 6 won't be ready), then reclaims POS 12>HUAFEI on IWIP. Only fresh LIM rides direct. 3.x scenarios keep TF>HUAFEI.
- RIM preferentially on FeNi routes (Killian).

### Implementation
- `scenario_api.py`: `ld_route_for_scenario(sid)` → "TF>POS 12" for S7/4.1, else "TF>HUAFEI". Constants `SAP_SALES_41_T` etc; `sap/tos/ld/total_target_for_scenario()`.
- `planning_rules.js`: `POS 12>HUAFEI` added to posTransit routes. `plan_sap_target.js` posTransit split is now **material-aware**: LD share of a dump's inflow rides its HUAFEI leg, SAP buffer share keeps FeNi legs.
- Draft flow: `POST /api/scenarios/S7/draft-plans {year:2026, day:7, months:[9,10,11,12], overwrite:true}` → then top up December LD rows to the full 1,281 pool → freeze via real UI flow.
- **Freeze script**: `/tmp/freeze_s7_all.py` (Playwright, headless chromium): load date → wait `_planDraft` fleet matches file → wait `_pathResp` non-null → computePlan → curves settle → planRunScenario → planAllocatePriority → planSaveForDate. Modeled on `scripts/refreeze_allocations.py`.

### Bugs found & fixed this session (all committed)
1. **August leak**: waterfall's chronological LD fill ran over `_MONTHS` (8–12) but S7 starts in Sept — August's backfilled S1 pool silently drank 1,288,393 t of the LD line. Months outside the scenario now get 0 share.
2. **LD targets were capacity guesses**: draft builder stamped `free_trucks × demonstrated_rate` (120/100 t/DT) as targetWmt instead of the month's share of the line. Now stamps waterfall's planned month t/day split by rated share (weights computed BEFORE the loop — reading `capacity{}` inside it gave the first contractor 100%).
3. **Stale constraints panel**: Year sheet said 3.x text (LD→HUAFEI, SAP 5,718,686) on a 4.1 workbook. Now scenario-aware (`is_41` in `_xlsx_scenario_constraints_block`).
4. **IWIP trips=0**: `rowTonnes()` prices trips via `planTripsPerDT`, which returns zero for routes outside the path model. Foreign rows now fall back to the stored `_transitTripsPerDt` sizing rate.
5. (Earlier) final-trim receiver re-pick, allocation.old drift, quiet-load refusing to replace non-empty draft (stamped Sept into all four dates — freeze script now verifies loaded fleet).

### Current verified state
- All four day-07 saves frozen; targets reconcile: SAP 6,383,383 / TOS 4,581,157 / LD 6,035,435 (rounding only).
- Results: SAP 100.2%, TOS 100.3%, **LD 137.6% before adjustment** (POS 12 is 40.8 km vs Huafei 63.7 — RIM 174 vs 97 t/DT/day, nearly double).
- **Park box: own 930 DT flat, park 702 truck-months (Oct 1, Nov 350, Dec 351), year lands 99.9%.**
- Workbook: `~/Downloads/plan_2026_S4_1.xlsx` (sheets: Year, Road crowding, Paths, Sep–Dec, Saturation). J72 green, QA 51/51.

## Known traps for the next agent
- **Frozen plan edits need THREE places patched**: `paths[k].dt`, `paths[k]._preAlloc.dt`, `allocation.rows[].dt_before` (restore sets dt=dt_before).
- Excel export endpoint is `/api/monthly/export-year` (NOT year-xlsx). Saved plans live in `data/saved_plans/` (gitignored).
- `planLoadSavedForDate({quiet:true})` will NOT replace a non-empty draft; non-quiet asks confirm() — auto-accept dialogs in Playwright.
- Never re-implement the allocator in Python — drive the browser (J52 lesson: a harness that constructs its own input can't catch bugs in what the real caller sends).
- Commit messages explain WHY; push BOTH remotes; refreeze via real UI flow; verify workbook cell-by-cell after changes.
- A parallel agent added the Saturation sheet + congestion_charts changes — don't clobber.

## Open items
- Night shift does −12.9% trips/DT (measured, 483k trips) but the model treats shifts identically → ~10% night over-prediction, documented not fixed.
- RIM/SMA rate inversion on TF>POS 12 in the frozen sim (plan says SMA 177 > RIM 113 t/DT; standalone model says RIM 174 > SMA 125) — traced toward the hybrid curve's per-contractor transform, not yet resolved.
- Huafei/BSE separation pending; possible new mine-plan revision incoming; 4.1 SAP leans on the 627 kt HGS stock top-up.
- Sanity-check with Killian: can the POS 12 corridor take ~85 kt/day LD + SAP buffer + tenants? (930-DT ownership number depends on it.)

---

## ADDENDUM 2026-09-01 (commit `37cb7b9`) — 19 Mt clock + T14 ruling
The state above is superseded on three numbers:
- **LIM-LD line is now 8,035,439** (+2,000,000 t, all TOFU LD reclaim; owner 2026-09-01). **TOTAL 19,000,001.** SAP/TOS lines unchanged.
- **Park box is now: own 1,182 DT flat, park 197 truck-months, year lands 99.9%** (LD 108.3% before adjustment). The 930/702 figures were the 17 Mt clock.
- **T14 (WB_IWIP_T14, km 26.1) is removed from every route whose DESTINATION is POS 12** (Hugo 2026-09-01: the yard is at km 27, trucks dumping there never pass it). Removed in `data/wb_register.json`, `fixtures/wb-allocation-basis.json`, and the new `data/weighbridge_matrix_owner_2026-09-01.tsv` (the `--register` generator picks the newest TSV, so regeneration keeps the ruling). T14 stays eligible where trucks DO pass it: POS 12>FENI reclaim legs, FeNi/BSE/HUAFEI rows.

New bug fixed on the way: `waterfall()` priced 4.1's LD capacity at the TF>HUAFEI matrix rates (63.7 km), understating the 40.8 km POS 12 leg — the month targets summed 279 kt short of the line (two clocks again). 4.1 now probes the path model (`monthly_api._path_row_wmt`) for the rate on `ld_route_for_scenario()`'s route.

Open item added: the mine plan's recovered stock available is 7,243,968 t; the 8,035,439 line overdraws it by ~791 kt. Manager to confirm the extra stock source.

Verified: day-07 saves refrozen via the real UI flow (Dec topped to 1,281), targets sum SAP 6,383,383 / TOS 4,581,157 / LD 8,035,414. Workbook `~/Downloads/plan_2026_S4_1.xlsx` regenerated and checked (destination-POS 12 rows show T15/T16 only). Gates J72 + J87 + QA 51/51 green.

---

## ADDENDUM 2 — Scenario 4.2 (S8, day-08), commit `6924bb1`
Client/commercial plant split (2026-09-01), built alongside 4.1 (day-07 untouched):
- **TOS 4,581,137 split 2/3 Huafei : 1/3 BSE every month** (client monthly table, exact 2:1).
- **LD 8,000,000**: 2.0 Mt DIRECT to yards (HUA 1.5 + BSE 0.5) + 6.0 Mt via POS (HUA 4.0 + BSE 2.0). TOTAL 18,964,562.
- **POS switch**: Sep–Oct LD transits TF>POS 12; **from Nov 1 LIM stocks POS 6 only** (both plants). SAP buffers keep POS 12/14 all year. Frozen IWIP legs verified: Sep = POS 12>HUAFEI+BSE, Nov/Dec = POS 6>HUAFEI+BSE.
- **BSE** = second HPAL plant, coastal cluster between Huafei and POS 14. Live SQL: ~360 kt measured Jan–Apr 2026, TF>BSE 1.31 trips/DT/day vs TF>HUAFEI 1.40.
- Model fixes: `planTripsPerDT` now prices unmeasured routes on the hybrid curve (was null → pred 0 → Allocate drained LD rows to 1 DT); waterfall capacity probe moved 300→150 DT (inside every leg's measured envelope).
- **Result: year 99.3% (18,838,039), LD 97.7% — UNDER its line by ~185 kt (mostly Oct–Nov), so the park box is empty.** The direct-yard legs are slower than 4.1's all-POS-12 run. OPEN: if the client insists on the full 8.0 Mt LD, the fleet ramp needs ~15–20 more trucks in Oct–Nov, or the shortfall stands as the honest answer.
- Workbook: `~/Downloads/plan_2026_S4_2.xlsx`. Gates J72 + J87 + QA 51/51 green.
